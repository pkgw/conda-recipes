# Copyright Peter Williams and collaborators.
# This file is licensed under a 3-clause BSD license; see LICENSE.txt.

{% set name = "rubbl-rxpackage" %}
{% set version = "0.1.6" %}
{% set sha256 = "882527973fdd8449937838a81d3ffe2d65c392b2b1684ebe117d98578b9d4457" %}

package:
  name: {{ name | lower }}
  version: {{ version }}

source:
  url: https://github.com/pkgw/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0

requirements:
  build:
    - rust <1.75
    - {{ compiler("c") }}
    - {{ stdlib("c") }}
    - {{ compiler("cxx") }}

test:
  commands:
    - rubbl-rxpackage flagts --help
    - rubbl-rxpackage peel --help
    - rubbl-rxpackage spwglue --help

about:
  home: https://github.com/pkgw/rubbl-rxpackage
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'Radio astronomy reduction utilities written in Rust, using the Rubbl framework'
  dev_url: https://github.com/pkgw/rubbl-rxpackage

extra:
  recipe-maintainers:
    - pkgw
