# Copyright 2014-2017 Peter Williams and collaborators.
# This file is licensed under a 3-clause BSD license; see LICENSE.txt.

{% set name = "gtk+" %}
{% set version = "3.22.30" %}
{% set version_majmin = '.'.join(version.split('.', 2)[:2]) %}
{% set sha256 = "a1a4a5c12703d4e1ccda28333b87ff462741dc365131fbc94c218ae81d9a6567" %}

package:
  name: gtk3  # note customization
  version: "{{ version }}"

source:
  fn: {{ name }}-{{ version }}.tar.xz
  url: http://ftp.gnome.org/pub/gnome/sources/{{ name }}/{{ version_majmin }}/{{ name }}-{{ version }}.tar.xz
  sha256: {{ sha256 }}
  patches:
    - m_pi.patch
    - no-module-warning.patch
  run_exports:
    - {{ pin_compatible('gtk3', max_pin='x') }}

build:
  number: 1
  script_env:
    - NJOBS

requirements:
  build:
    - pkg-config
    - pthread-stubs
    - python
    - {{ compiler('c') }}
  host:
    - xorg-xproto
    - xorg-inputproto
    - at-spi2-atk
    - atk 2.25.*
    - cairo
    - epoxy 1.*
    - fontconfig
    - gdk-pixbuf 2.36.*
    - glib {{ glib }}
    - gobject-introspection 1.*
    - pango {{ pango }}
    - xorg-libice 1.0.*
    - xorg-libsm 1.2.*
    - xorg-libx11 1.6.*
    - xorg-libxi 1.7.*
    - xorg-libxrender 0.9.*
  run:
    - atk 2.25.*
    - gdk-pixbuf 2.36.*
    - glib {{ glib }}
    - gobject-introspection 1.*
    - pango {{ pango }}
    - xorg-libice 1.0.*
    - xorg-libsm 1.2.*
    - xorg-libx11 1.6.*
    - xorg-libxi 1.7.*
    - xorg-libxrender 0.9.*

about:
  home: http://www.gtk.org/
  license: LGPL
  summary: version 3 of the Gtk+ graphical toolkit
